# DK Bukittinggi — Starter repo

Folders:
- mobile/  (React Native app)
- backend/ (Express + Socket.IO + Prisma)
- admin/   (Next.js admin panel)

Quickstart:
1. Clone repo
2. Setup .env for backend (DATABASE_URL, MIDTRANS_SERVER_KEY, SOCKET_URL)
3. cd backend && npm install && npm run dev
4. cd admin && npm install && npm run dev
5. cd mobile && npm install && expo start
